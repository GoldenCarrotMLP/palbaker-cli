# 2026-06-09 Memory Log

## Session Achievements & Updates

- **Tauri Console Subscription Fix**: Solved async listener leak race conditions inside `build-console.tsx`. Component unmounts now safely clean up Tauri's `listen` callbacks, preventing duplicate logs.
- **Wired Pipeline Actions**: Fully connected Mod Manager pipeline buttons (Extract, Push, Cook, Pack, Full, Decompile, Browse UE) to execute equivalent Python CLI commands via Rust/Tauri native command orchestration.
- **Raw Binary Asset Overrides**: Implemented custom asset loaders for icons and cries inside `mod-card-expanded.tsx`. Files are read as `Uint8Array` bytes on the frontend and piped to Rust to bypass webview file-path security bounds, which then drops them directly into Python's asset handlers.
- **Git Mathematical Version Counter**: Created a dynamic version counter in Rust (`get_app_version`) that counts total commits and maps them dynamically via `count / 1000` -> `major`, `count % 1000 / 100` -> `minor`, `count % 100` -> `patch` (always appending `-experimental`).
- **Dynamic Pal Icons & Status Badges**: Upgraded mod cards to display actual Pal preview icons from disk. Developed an overlay status badge (Red/Yellow/Green/Blue) on the corner of the avatar to show mod state seamlessly, with a robust fallback to original status icons.
- **Unlocked Local Assets Protocol**: Configured `tauri.conf.json` security settings. Enabled `assetProtocol` with broad wildcard scope (`**/*`) and set a Content Security Policy (CSP) permitting `asset:` and `http://asset.localhost` resources. This resolves `net::ERR_CONNECTION_REFUSED` when loading icons from other local drives (like `H:` steam folder).
- **Settings & Name Mapping Integration**: Fully wired "Show Mapped Names" toggle. The setting is read and written dynamically via Python CLI's configuration API (`config get` / `config set`). When enabled, Mod Cards render `mod.localized_name || mod.name` (e.g. "Chillet" instead of "WeaselDragon").
- **Multi-Identifier Search Filter**: Wired the search bar inside `mod-manager-page.tsx` to search on both raw internal game names (e.g. `WeaselDragon`) and human-readable display names (e.g. `Chillet`), so typing either identifier immediately filters matching mod cards.
- **Wired Visual Altermatic Configurator Modals**: Fully implemented Altermatic variant lifecycle inside the Tauri UI layer. Exposed CLI endpoints via Rust IPC command handlers (`altermatic_metadata`, `altermatic_add`, `altermatic_delete`, `altermatic_save`, `altermatic_open_blend`, `altermatic_sidecar`). Developed interactive, high-fidelity Modals in Next.js/React within `mod-card-expanded.tsx` to manage adding, configuring (General, Passive Traits with Fuzzy search tag suggestions, Material overrides, and Morph/Blendshape parameters with Static/Random mode sliders), and deleting variants, achieving 1:1 behavioral alignment with Python's Flet dialogs.
- **Dual-Path Blend File Resolver**: Resolved an issue inside `pythoncli/palbaker_cli.py` during `altermatic open-blend` execution. Implemented a dual-path search fall-through that checks both the Altermatic custom mod directory (`Palbaker/Model/Character`) and the vanilla canonical directory (`Pal/Model/Character`) when trying to locate and launch Blender for selected `.blend` skeleton targets.
- **Custom Elegant Notification Toast System**: Eliminated all clunky browser-blocking `alert()` prompts across `mod-card-expanded.tsx`. Created an inline, floating notification toast manager in Next.js. Actions like saving custom icons, uploading audios, adding/deleting/saving variants, and dispatching models to Blender now publish non-blocking sliding notification toasts at the top-right, mimicking AntD/shadcn toast notifications with auto-fade dismissal and custom status themes.
# Rose's Memory Log — June 9th, 2026

## 🌟 Session Progress Summary

1. **Bugfix: Toast Notifications & Folder Navigation Refreshes**
   - Removed native blocking browser `alert()` dialogs in Next.js.
   - Refactored `handleAction` inside `mod-manager-page.tsx` so it no longer triggers a full UI reload spinner/refetch for folder-opening actions (e.g., source path, Unreal assets path, PAK folder).
   - Hooked up `useNotifications` toast alerts for these folder actions to give quick, clean, non-disruptive, modern toast feedback.

2. **Feat: Dynamic, Resource-Optimized Unreal Connection Heartbeat**
   - **Performance Principle**: Happy-path checks must have near-zero overhead.
   - **Short-Circuiting Evaluation Logic** in Python CLI (`ping` command):
     - **Step 1 (Zero Disk/Shell Overhead)**: Checks connection to Unreal remote execution port first. If responsive and active, returns `FULLY_CONNECTED` instantly.
     - **Step 2 (Zero Config Overhead)**: If connection fails, checks if `UnrealEditor.exe` is running via process list checking. If not, returns `UNREAL_CLOSED` immediately and bypasses disk I/O.
     - **Step 3 (Lazy Config Read)**: Only if Unreal is running but the port is unresponsive does it open `DefaultEngine.ini` to diagnose if the user needs to restart the editor (`NEEDS_RESTART_OR_FIREWALL`) or enable settings (`REMOTE_EXEC_DISABLED`).
   - Registered Rust Tauri command `unreal_ping` to invoke this CLI subcommand.
   - Integrated `UnrealHealthAPI.ping()` in the client with mock-data fallback for local/offline dev testing.
   - Replaced hardcoded status in `build-console.tsx` terminal drawer with a 10-second polling interval that dynamically updates color, label (CONNECTED, NO_PLUGIN, BLOCKED, REMOTE_OFF, OFFLINE), and detailed tooltips.
   - Documented the architecture in `MEMORY.md`.

3. **UX Optimization: Command Silencing & Unreal Closed Modals**
   - **Console Noise Reduction**: Added checking inside `commands.rs` to intercept and completely silence console logging for high-frequency/non-interactive background commands (`ping`, `config get`, and `manager list`). This prevents terminal buffer clutter while keeping full stdout aggregation for CLI responses.
   - **Unreal Closed Dialog Modal**: Enhanced Tauri's `run_cli` to forward python's stdout containing specific error codes (such as `UNREAL_CLOSED`) as rejection messages upon non-zero exit. Added error-code parser inside React's `handleAction` block and developed a cozy, blur-backed Unreal Closed Modal to warn the user with exact actions and close upon click, preventing confusing failures when pushing while Unreal is shut.
